const checkMessageToxicity = async (message, toltoleranceLevel) => {
	console.log(message, toltoleranceLevel);
	// pass the message to the model
	return true;
};

module.exports = checkMessageToxicity;
